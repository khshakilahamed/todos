export const PRIORITY = {
  EXTREME: "extreme",
  MODERATE: "moderate",
  LOW: "low",
} as const;

export const AUTH_KEYS = {
      ACCESS_TOKEN: 'accessToken',
      REFRESH_TOKEN: 'refreshToken',
      USER: 'user',
} as const